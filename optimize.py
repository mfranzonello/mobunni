from scipy.optimize import minimize

servers = 5*[{'enclosures': 6, 'nameplate': 250}]
frus = [25, 50, 37, 50, 44, 0,
        0, 37, 47, 46, 39, 31,
        30, 0, 23, 50, 34, 41,
        40, 26, 34, 35, 45, 33,
        0, 50, 30, 47, 40, 39]

def server_loss(nameplate, frus):
    return

def  find_solution(servers, frus):
    return

